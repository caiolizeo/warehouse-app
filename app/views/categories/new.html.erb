<h1>Nova categoria</h1>

<ul>
  <% @category.errors.full_messages.each do |msg| %>
    <li><%=msg%></li>
  <%end%>
</ul>

<%= form_with(model: @category) do |f| %>
  <div>
    <%= f.label :name, 'Nome'%>
    <%= f.text_field :name %>
  </div>

  <%=f.submit 'Gravar'%>
<% end %>